---
version: 1.0
name: Boli Creative Sync Design System
description: 素材悬浮上传是一个给发行设计师日常使用的 Windows 常驻小工具。界面应像轻量生产工具，而不是后台管理页、营销页或大屏仪表盘。整体以白色为主，粉色仅作为动作、选中、状态和波利品牌感的点缀色。

colors:
  accent: "#fe9daf"
  accent-hover: "#ff8fa8"
  accent-pressed: "#f27f9a"
  accent-soft: "#fff0f4"
  accent-surface: "#fff7fa"
  accent-border: "#ffd0dc"
  background: "#f8f9ff"
  window: "#ffffff"
  surface: "#ffffff"
  surface-soft: "#f7f9fc"
  surface-muted: "#eef4fe"
  border: "#dde3ed"
  border-soft: "#edf0f5"
  text: "#161c23"
  text-muted: "#647084"
  text-subtle: "#8a93a3"
  success: "#27c46a"
  warning: "#f6b44b"
  error: "#e45b68"
  overlay-handle: "#fe9daf"

typography:
  font-family: "Microsoft YaHei UI, Segoe UI, Inter, system-ui, sans-serif"
  title:
    fontSize: 16px
    fontWeight: 700
    lineHeight: 22px
    letterSpacing: 0
  section-title:
    fontSize: 14px
    fontWeight: 700
    lineHeight: 20px
    letterSpacing: 0
  body:
    fontSize: 13px
    fontWeight: 500
    lineHeight: 18px
    letterSpacing: 0
  body-regular:
    fontSize: 13px
    fontWeight: 400
    lineHeight: 18px
    letterSpacing: 0
  label:
    fontSize: 12px
    fontWeight: 600
    lineHeight: 16px
    letterSpacing: 0
  caption:
    fontSize: 11px
    fontWeight: 400
    lineHeight: 15px
    letterSpacing: 0

radii:
  window: 24px
  panel: 16px
  card: 12px
  control: 10px
  thumbnail: 8px
  pill: 999px

shadows:
  window: "0 24px 70px rgba(31, 35, 44, 0.18), 0 8px 24px rgba(31, 35, 44, 0.10)"
  floating-boli: "0 10px 26px rgba(254, 157, 175, 0.35), 0 3px 10px rgba(31, 35, 44, 0.16)"
  preview: "0 18px 36px rgba(28, 35, 48, 0.12)"
  button: "0 10px 24px rgba(254, 157, 175, 0.32)"
---

## 产品定位

这个工具服务的是“做完图之后立即处理并入表”的高频动作。设计师不应该感觉自己在填后台表单，而是在把素材交给一个常驻助手：拖进去、确认字段、必要时微调叠加层、上传并重命名。

首屏必须直接进入工作流，不做欢迎页、功能介绍页、营销式 hero 或大面积说明文字。

## 视觉原则

- 主色是白色，粉色 `#fe9daf` 只用于主按钮、选中态、开关、当前帧、当前队列、波利品牌点缀。
- 任何页面都避免蓝色、橙色、紫色渐变成为主视觉。
- 界面要轻、干净、紧凑，像一个 Windows 浮窗工具。
- 边框要弱，阴影只用于窗口本体、悬浮波利、主预览图和主按钮。
- 不使用装饰性大背景、光斑、渐变球、营销卡片。
- 不做卡片套卡片。只有队列项、叠加图层控制、设置分组可以是卡片。

## 主窗口结构

默认窗口尺寸以 `1024 x 768` 为基准，圆角 `24px`，带清晰外投影。打开后所有核心区域应完整显示：

1. 顶部栏：设计师选择、项目选择、同步、设置、收起/关闭。
2. 左侧上传队列：缩略图、文件名、状态、完成标识。
3. 中央预览区：当前图片或视频截图，保持较大视觉权重。
4. 右侧叠加图层：Logo、Slogan、Icon 三个控制块。
5. 下方字段区：语言、尺寸、完成日期、创意、素材形式、素材方向。
6. 底部操作栏：处理数量提示、主按钮。

不要把字段挤到预览图上方，也不要让右侧图层面板比预览图更抢眼。

## 顶部栏

顶部栏高度约 `56px`。左侧两个下拉控件分别是设计师和项目：

- 控件本身不要画很重的边框，只用轻分隔线或淡背景。
- 文字使用 `13px / 14px`，加粗但不要过大。
- 下拉箭头必须明显，整块区域都可点击，不只箭头可点击。
- 项目列表只展示允许写入的项目表，避免误选无关表。

设置页顶部栏进入设置模式时必须变化：左侧显示返回按钮、标题“设置”、副标题“素材悬浮上传”。不要继续展示主界面的设计师和项目切换。

## 上传队列

左侧宽度约 `180-200px`。队列项包含：

- 40px 左右缩略图。
- 文件名一行截断。
- 状态一行：等待上传、处理中、已完成、失败。
- 删除按钮和拖拽排序手柄。
- 当前项使用粉色浅底 `#fff0f4` 和粉色边框。
- 当图片字段、叠加层都已满足上传条件时，显示小完成标识。

队列区域不要使用横向滚动条。

## 预览与叠加层

预览图是主视觉，默认方形图以 `420-440px` 展示，视频截图界面可根据内容稍大。图片本身需要保持原比例，不裁切主体。

叠加层要求：

- Logo、Slogan、Icon 都按透明 PNG/WebP 素材绘制，不加白底、不加半透明背景。
- 选中叠加层时显示粉色描边和四角缩放点。
- 支持拖动位置、四角缩放、保持比例。
- 新加入队列的图片默认继承上一张已选的叠加素材和开关状态。
- 右侧开关打开时，控件使用粉色；关闭时使用浅灰。
- 叠加层默认尺寸可以偏大，方便用户直接看见并缩放。

## 字段区

字段区使用两行三列，贴近参考图的轻量输入框：

- 标签浮在输入框上边缘，字号 `11-12px`。
- 输入框高度约 `38-42px`。
- 默认日期永远是今天。
- 尺寸默认 `F`。
- 素材形式/素材方向需要兼容不同表里的同义字段，例如“素材内容/素材形式”“细分内容/素材方向”。
- 素材方向不同步时，要在输入框内给出清楚但克制的提示，不要弹大提示打断流程。

## 视频截图界面

拖入 MP4/H.264 视频时进入视频取帧界面。整体结构保持与 Stitch 参考一致：

- 顶部：返回按钮、视频文件名、同步、设置、收起。
- 左侧大预览：显示当前帧，保留时间码。
- 右侧已选截图：标题、数量、清空列表、截图卡片、删除按钮。
- 下方 timeline：类似剪辑软件，一整条视频缩略图时间轴，不使用分散按钮式帧列表。
- 红色播放头可点击/拖动移动时间。
- 按住 Ctrl + 鼠标滚轮缩放时间轴。
- 键盘 `+` / `-` 或方向键用于逐帧移动，不在界面上放大量“-1帧/+1帧”按钮。
- 底部主按钮为“生成截图并加入队列”，选中帧为空时禁用。

视频截图加入主队列后，素材形式默认写“视频截图”。

## 悬浮波利

悬浮波利是入口，不是窗口装饰。

- 尺寸建议 `72-92px`，根据屏幕缩放保持清晰。
- 不显示“拖图给我”等文字气泡。
- 常驻后台时只显示系统托盘图标和悬浮波利，不在任务栏显示。
- 点击打开主窗口；长按后才进入拖动状态；松开立即停止跟随鼠标。
- 拖图片/视频靠近时播放张嘴待接收状态，放下后播放吃掉动画。
- 被点击时先播放一次“被点击”序列，再进入按住循环状态。

## 设置页

设置页仍在同一窗口内，不弹大后台页。布局：

- 顶部设置模式栏：返回、标题、副标题、右侧同步/收起。
- 内容区宽度居中，设置分组纵向排列。
- 底部固定操作栏：取消、保存设置。

设置分组：

- 飞书连接：App ID、App Secret、App Token、当前数据表。
- 工作流偏好：当前项目输出目录、全局默认输出目录、按年份/月自动分类、开机自动启动、关闭窗口时常驻后台。
- 本地素材库：Logo、Slogan、Icon 文件夹。
- 默认字段：设计师、语言、尺寸、素材形式、素材方向。

保存按钮始终在底部右侧，不放在页面中间。

## 组件规范

### 主按钮

- 背景：`#fe9daf`
- 文字：白色
- 圆角：`999px`
- 高度：`48-52px`
- 阴影：粉色轻投影
- 图标：使用上传/下载相关线性图标

### 次按钮

- 白底、浅边框、深色文字。
- 用于取消、返回、选择文件夹等低风险动作。

### 输入框/下拉框

- 白底或极浅灰底。
- 边框 `#dde3ed`。
- 聚焦时边框变粉色。
- 整个输入区域都可点击。

### 开关

- 开启：粉色轨道，白色圆点在右。
- 关闭：浅灰轨道，白色圆点在左。
- 不使用蓝色开关。

### 图标

- 优先使用 Lucide 或 Material Symbols 的线性图标。
- 图标颜色默认跟随文字色，主动作图标可用白色。
- 托盘图标使用 `迷你.icon.png` 风格的小波利图标。

## 交互原则

- 拖拽图片：进入主图片处理界面。
- 拖拽视频：进入视频截图界面。
- 图片和视频必须按扩展名/MIME 清楚区分。
- 所有下拉选择整块可点击，不要求点箭头。
- 批量队列中第一张设置的叠加层，应作为后续图片默认值。
- 输出前先向飞书创建记录，拿到“素材完整命名”，本地先重命名/生成最终图，再用该文件名上传附件。
- 输出目录支持按项目设置，并可按 `YYYY年/MM月` 自动归档。
- 避免系统文字被选中，尤其是悬浮波利、timeline、队列拖拽区域。

## 质量检查

每次 UI 调整后至少检查：

- `1024 x 768` 主窗口核心内容完整显示。
- 设置页底部保存栏没有遮挡输入内容。
- 图片预览不变形，透明 PNG 没有白底。
- 视频 timeline 可拖动、可 Ctrl 缩放，且没有蓝色文本选中。
- 主色没有偏成蓝色/橙色/紫色。
- 按钮文字不溢出，不使用负字距。
- 悬浮波利不在任务栏显示，托盘图标可见。

## 禁止事项

- 禁止做欢迎页、落地页、大 hero、营销文案卡片。
- 禁止用蓝色作为主按钮或主要选中态。
- 禁止给透明 Logo 自动加白底、玻璃底或半透明底。
- 禁止把大量说明文字塞进主流程。
- 禁止在主窗口里套多层卡片。
- 禁止让设置页沿用主界面的顶部项目选择栏。
